package main

/*
There are no Do loops or While loops in Go.
There are no Ternary statements in Go.
*/
/*func main() {

	var a, b int

	//if statement requires braces samelined, because Go inferrs semicolons
	if a > b {
		//do stuff
	} else if b > a {
		//do other stuff
	} else {
		//do other other stuff
	}

	//if can have a basic initialization or function
	if c := 2; a > c {
		//do stuff
	}

	//Switch statements very general, solved top to bottom. If not given a parameter, switch is
	//just a fancy if-else-if-else block.
	switch a {
	case 0, 1, 2:
		a = 4
	default:
		a = 0
	}

	switch {
	case a > b:
		a = 4
	case b > a:
		a = 3
	default:
		a = 0
	}

}*/
